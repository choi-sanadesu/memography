---
description: "Ingest a source from raw/ into the wiki. Usage: /bookkeeper:ingest <filename>"
---

Ingest the source `$ARGUMENTS` from the `raw/` directory of the current vault (CWD).

Follow the **Ingest** operation in the bookkeeper skill exactly. If the vault scaffold is missing, run the **Scaffold (initialization)** safety check and create the layout first.
