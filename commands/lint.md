---
description: Health-check the wiki for contradictions, stale claims, orphans, and missing pages.
---

Run a health check on the wiki in the current vault (CWD).

Follow the **Lint** operation in the bookkeeper skill exactly. If the vault scaffold is missing, run the **Scaffold (initialization)** safety check and create the layout first.
