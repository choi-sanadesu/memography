---
description: "Query the wiki and answer with [[wikilink]] citations. Usage: /bookkeeper:query <question>"
---

Answer the user's question using the wiki in the current vault (CWD): `$ARGUMENTS`

Follow the **Query** operation in the bookkeeper skill exactly. If the vault scaffold is missing, run the **Scaffold (initialization)** safety check and create the layout first.
